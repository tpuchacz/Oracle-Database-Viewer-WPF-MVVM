<UserControl x:Class="MVVMToolkit.DatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMToolkit"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance Type=local:DatabaseViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <ListBox Width="120"
                     ItemsSource="{Binding Path=Tables, UpdateSourceTrigger=PropertyChanged}"
                     IsSynchronizedWithCurrentItem="True" FontSize="12" Height="120" />
        </StackPanel>
        <TextBlock Text="{Binding ErrorMsg}" TextWrapping="WrapWithOverflow"></TextBlock>
        <Button Grid.Column="1" Width="100" Height="30" Content="Ładuj dane" Command="{Binding LoadDataCommand}"/>
        <DataGrid ItemsSource="{Binding Path=OracleDBData}" Grid.Column="2" AutoGenerateColumns="True"/>
    </Grid>
</UserControl>
